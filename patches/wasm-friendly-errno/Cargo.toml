[package]
name = "errno"
version = "0.3.14"
edition = "2021"
authors = ["Atlas Sphere <dev@atlas-sphere.io>"]
license = "Apache-2.0"
description = "Tiny wasm-friendly stub of errno for wasm builds (patch replacement)"

[lib]
crate-type = ["rlib"]

[features]
default = ["std"]
std = []
